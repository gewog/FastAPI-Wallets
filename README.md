# FastAPI-Wallets — Backend для управления кошельками пользователей

> **Цель проекта**: Создание надёжного и масштабируемого backend-сервиса для управления кошельками пользователей с использованием FastAPI, PostgreSQL, Alembic, Poetry и Docker. Проект поддерживает конкурентные операции с балансом, автоматизированное тестирование и CI/CD.

---

### Стек технологий
- **Python 3.13**
- **FastAPI** — асинхронный веб-фреймворк для API
- **Uvicorn** — ASGI-сервер
- **Pytest**, **httpx** — тестирование
- **GitHub Actions** — CI/CD
- **Poetry** — управление зависимостями
- **Docker** — контейнеризация
- **PostgreSQL** — база данных
- **SQLAlchemy 2.0** — ORM
- **Alembic** — миграции базы данных

---

### Файлы проекта
| Файл | Назначение |
|------|------------|
| `app/main.py` | Основной файл приложения с маршрутами |
| `app/routers/wallets.py` | Эндпоинты для работы с кошельками |
| `app/models/wallet.py` | Модель кошелька для SQLAlchemy |
| `app/backend/db.py` | Подключение к базе данных |
| `app/backend/settings.py` | Настройки приложения |
| `app/backend/db_depends.py` | Зависимость для работы с сессиями SQLAlchemy |
| `tests/test_wallets.py` | Тесты для эндпоинтов |
| `pyproject.toml` | Конфигурация Poetry |
| `poetry.lock` | Зафиксированные версии зависимостей |
| `Dockerfile` | Сборка Docker-образа |
| `.github/workflows/ci.yml` | CI/CD пайплайн |
| `docker-compose.yml` | Запуск PostgreSQL и приложения |
| `alembic.ini` | Конфигурация Alembic |

---

### 🛠 Как запустить локально

#### 1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш_профиль/FastAPI-Wallets.git
cd FastAPI-Wallets
```

#### 2. Установите Poetry:
```bash
git clone https://github.com/ваш_профиль/FastAPI-Wallets.git
cd FastAPI-Wallets
```

#### 3. Установите зависимости:
```bash
poetry install
```
#### 4. Создайте .env файл:

   Создайте файл `app/backend/.env` с следующим содержимым:
   ```env
   DB_USER=postgres
   DB_PASS=22iamgewog22
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=wallet_db
   ```

#### 5. Примените миграции:
```bash
poetry run alembic upgrade head
```

#### 5. Запустите приложение:
```bash
poetry run uvicorn app.main\:app --host 0.0.0.0 --port 8000 --reload
```

### Проверьте работу:
- Swagger UI: http://localhost:8000/docs
- Создание кошелька: POST /api/v1/wallets/
- Пополнение/снятие: POST /api/v1/wallets/{wallet_id}/operation
- Получение баланса: GET /api/v1/wallets/{wallet_id}

### Как собрать Docker-образ

#### 1. Соберите образ:
```bash
docker build -t fastapi-wallets\:latest .
```
#### 2. Запустите контейнер:
```bash
docker run -p 8000:8000 fastapi-wallets\:latest
```
#### 3. Или используйте Docker Compose:
```bash
docker-compose up --build
```

### Как работает CI/CD
При каждом `push` или `pull_request` в ветки `master` или `develop`:

1. **Запуск PostgreSQL в Docker-контейнере** как сервис для тестирования.
2. **Клонирование репозитория** на виртуальную машину GitHub Actions.
3. **Установка Python 3.13** и необходимых инструментов.
4. **Установка Poetry** для управления зависимостями.
5. **Установка зависимостей проекта** через Poetry.
6. **Применение миграций Alembic** к тестовой базе данных PostgreSQL.
7. **Запуск тестов** с использованием `pytest`, которые проверяют функциональность приложения, взаимодействуя с запущенной базой данных PostgreSQL.

✅ Все шаги выполняются автоматически.

### Тестирование
Запуск тестов:
```
poetry run pytest tests/
```

Тесты проверяют:
- Создание кошелька
- Пополнение и снятие средств
- Обработку ошибок (недостаточно средств, несуществующий кошелёк)
- Удаление кошелька

### 🔗 Полезные ссылки
- [FastAPI](https://fastapi.tiangolo.com/)
- [SQLAlchemy](https://www.sqlalchemy.org/)
- [Alembic](https://alembic.sqlalchemy.org/)
- [Poetry](https://python-poetry.org/)
- [Docker](https://www.docker.com/)
- [GitHub Actions](https://docs.github.com/en/actions)

### 📝 Автор
gewog  
📧 gewoggewog@gmail.com

### 📄 Лицензия
Проект распространяется под лицензией MIT.